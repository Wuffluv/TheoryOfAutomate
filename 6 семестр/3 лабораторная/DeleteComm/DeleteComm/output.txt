print()
client_socket, client_address = server_socket.accept()
print(f)


def send_messages():
    while True:
        message = input()
        if message.lower() == :
            client_socket.sendall(message.encode())  
            client_socket.close()  
            break
        client_socket.sendall(message.encode())

 или в двойных кавычках 

в утроенных одинарных кавычках much 

или в утроенных двойных кавычках Abracadabra.